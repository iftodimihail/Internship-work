<%= form_with(model: t_user, local: true) do |form| %>
  <% if t_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t_user.errors.count, "error") %> prohibited this t_user from being saved:</h2>

      <ul>
      <% t_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
